# README.md — review-contract loop log

`README.md` is this project's design contract, so it is gated like one. Its
review history is kept here rather than appended to it: a review table is not
what a README's readers came for, and appending one changes what the document
is.

Each row is one `review-contract` loop. Rows are appended, never edited.

| # | Date | Genre | Lanes | Q1 | Q2 | Q3 | Verified | Fixed | Outcome |
|---|------|-------|-------|----|----|----|----------|-------|---------|
| 1 | 2026-09-08 | adr (pinned) | 3 | 2 | 1 | 0 | 3 | 3 | First gate on this document, armed by the rewrite for a lay reader. **All three lanes independently found the window-selection defect**, and it is the run's most consequential: the document said demoreel "locates the window by its name" and never stated that the biggest window wins, so an implementer building from the contract would pick by search order and get a startup dialog about half the time — the exact defect the code's docstring says it avoids, and the `--window-name` filter the scope ceiling rules out. **All three also found the concurrency claim**, which this rewrite had itself over-broadened: the previous text carried the qualification and the rewrite dropped it, so the document claimed a second unnamed run always refuses, when two started together both record and only the later is reachable by `stop` (DEMO-0011). **The Q2 was against the versioning standard**: step 6 stated the blank check unconditionally while the code skips it once the app has exited, and that exception is a protected surface — a conformer would have made it unconditional believing it a bug fix, and broken runs that pass today. One dismissed as immaterial: the no-window message reports a timeout rather than a nameless window, and nothing is built differently. Three open questions resolved clean and are not counted. One out-of-scope finding, fixed at its home: a `ci.sh` comment left stale by DEMO-0021 still said the workflow matches against the glob itself. **All three lanes disclosed that they were not cold** — the harness injects this project's `CLAUDE.md`, which restates parts of this document. |
| 2 | 2026-09-08 | adr (pinned) | 3 | 3 | 1 | 2 | 6 | 6 | **VIOLENT by § At the cap's measure — five of six landed on text this run wrote**, which for a document rewritten wholesale is the expected shape rather than a surprise. Two came from loop 1's own fix: step 6 said "nothing was ever drawn" when `display_is_blank` grabs ONE frame from the live screen at the end, so the correct form is an end state rather than a history — and the `--settle` paragraph still asserted the check unconditionally, contradicting the exception loop 1 had just added to step 6. Three came from the rewrite: `--version` sat in a table whose every other row is a `record` flag, when the code registers it on the top-level parser, so a conformer would put it on the wrong parser and `demoreel record --version` would fail; "the technical logs" had no antecedent two lines under "keep the log and check it", where an implementer could read it as licence to delete the caller's own `--app-log`; and the documentation-only push was described as two steps when `./ci.sh --docs` runs three, with the drift check attributed to "the full gate" when `ci.sh` deliberately runs it in BOTH modes because the glob is what selects the mode. The sixth was pre-existing and empirical: the `-d` row said "how many seconds to record" while the duration clock starts after the scripted steps — measured at `-d 4` giving 5.2s alone and 10.2s with `-a 'wait 5'`. Nothing dismissed. Three open questions resolved clean, one by measurement (demoreel is on `PATH` via a symlink in `~/.local/bin`). Two out-of-scope defects fixed at their homes rather than carried in: `ci.sh`'s required-programs check never gained `xauth` when DEMO-0017 added it to the tool, so a machine without it passed that step and failed later; and `SECURITY.md` claimed both backends start with `-nolisten tcp`, which is true only of `Xvfb` — replaced with a measurement, `ss -ltn` showing no X server opening a TCP listener before or during a `--gpu` run. **All three lanes again disclosed they were not cold.** |
| 3 | 2026-09-08 | adr (pinned) | 3 | 3 | 0 | 2 | 5 | 5 | **Cap reached (3 for an ADR); the run files its tail and exits. CALM.** The share measurement that usually decides this cannot: loop 1 was armed by a rewrite of the WHOLE document, so every anchor falls inside the gated span and "landed on text this run wrote" is true of almost everything by construction. Read on substance instead, the picture is the opposite of oscillation — this loop's findings are pre-existing omissions the plain-language rewrite exposed, not repairs of earlier repairs, and no finding here landed on a passage loop 2 fixed. **All three lanes independently found the quick-start snippet**, and it is the run's most consequential: `demoreel record … &` immediately followed by `demoreel stop mydemo` cannot work, because a run is addressable only once it writes its state file, which is after the window appears and ffmpeg starts. Reproduced verbatim — exit 1, "no recording named 'mydemo' is running", and the recorder left running. It was in the document before the rewrite too. The snippet now separates the two steps, says why, and shows a retry for scripts; the retry was executed before it was written down. **Two Q3s were consequences the contract left unstated.** "The private screen is *found*, never fixed" is satisfied by scanning for a free number — which is the exact race the sentence exists to close — so the mechanism is now named (`-displayfd`, and Xwayland's own inside `xwfb-run`) with scanning ruled out, matching the non-negotiable in `CLAUDE.md`. And "`./ci.sh` is the whole gate" never said the gate records on the ordinary backend only: no step runs `--gpu` (verified, zero occurrences in `ci.sh`) and the Flatpak step uses a stand-in, so a maintainer could ship a black `--gpu` video on a green gate. **A third Q1 came from reading the wait loop**: `-d 0` was documented as "until I say stop" when the loop also breaks when the app closes itself. **One fixed that two lanes had judged immaterial**: the plain-language gloss said no other program on the machine can peek, when the cookie stops another *user* and not another process running as you — fixed because `SECURITY.md` states it precisely and the two documents disagreeing is a Q2 waiting to happen. Nothing dismissed. Surfaced code-side, not fixed here: an app that names no window and exits 0 before the timeout takes the single-instance error message, which may be the wrong diagnosis for it. **All three lanes again disclosed they were not cold.** |
| 4 | 2026-09-25 | adr (pinned) | 3 | 0 | 0 | 3 | 3 | 3 | Armed by DEMO-0100 (1c1b390): a new "Taking a picture" section for `demoreel shot`, which changes what the contract says the tool makes. Loop 1 of this run. Every lane held every question. **All three lanes found the same two gaps.** First, "a `shot` never collides" had nothing behind it. Every run keys its files on `--name` and `stop` finds runs by scanning state files, so a builder would either reuse the default name, colliding with an unnamed `record`, or invent a key `stop` might then find. Line 292's "Everything a run writes is keyed to its name" also contradicted it. Now: a `shot` keeps its files in a folder of its own and writes nothing `stop` reads, and line 292 is scoped to recordings. Second, "fails just as a blank recording does" read two ways, because a recording is excused when the app closed itself. Now: the picture is always checked, a failed look fails too, and a failed run prints no path. **One lane found the third**: the default filename is per-second, so two parallel shots of one app share it. That half of the no-collision claim is narrowed, with `-o` named as the remedy. Nothing dismissed. Two open questions resolved clean: `stop` finds runs through `*.json` state files (`live_runs`), and a `--settle` timeout on `shot` is the builder's to order. Code-side, filed for the build: `ci.sh`'s flag check reads only `record` and `stop` help, so it will not see `shot`'s flags until it does. **All three lanes disclosed they were not cold**: the harness injects this project's `CLAUDE.md`, and the git snapshot named the arming commit. |
| 5 | 2026-09-25 | adr (pinned) | 3 | 0 | 1 | 0 | 1 | 1 | Loop 2 of the DEMO-0100 run. Every lane held every question. **All three lanes found the same one defect, and it was loop 1's own text**: the new `shot` paragraph said its private files were "removed when it ends", against the log rule at the `--app-log` section that demoreel's own logs are kept when a run fails. A builder following the first deletes the display log on every exit, so a failed `shot` leaves nothing to read. Now: removed when it succeeds, kept when it fails, like the logs below. In the same passage, "leaves the picture on disk" could not hold when the screen could not be read at all, so there is no picture. Two lanes raised it as an open question and it is fixed by narrowing: a failed run keeps any picture it had saved. **Stopped here, one loop short of the ADR cap of 3.** The own-fix share is 1 of 1: the loop found nothing but a repair of loop 1's repair. The armed change was a whole new section, so the share is weaker evidence than usual, but read on substance this is a repair of a repair, not a pre-existing defect the change exposed. Nothing verified is unfixed, so there is no tail. Second share: 4 of the run's 4 verified findings fall inside the gated span, which says little, since the span was the new section. Open questions resolved as the builder's: which frame is checked (the build checks the frame it saves), and whether a `--settle` timeout on `shot` stops or carries on. Code-side, carried to the build: `ci.sh`'s flag check must read `shot --help`. **All three lanes disclosed they were not cold**: the harness injects this project's `CLAUDE.md`, and the git snapshot named loop 1's commit and its three topics. |
| 6 | 2026-09-26 | adr (pinned) | 3 | 4 | 0 | 2 | 6 | 5 | Armed by DEMO-0111 (f5812f0 on branch demo-0111): `--gpu` records from the compositor with `wf-recorder` on a `cage` and `Xwayland` demoreel starts itself. The document leads the code: it is the design, gated before the build. Loop 1 of this run. Every lane held every question. **All three lanes found that the `--cursor` refusal did not say whether it covers `shot`**, whose X-side capture can draw the pointer. Now `record --gpu` refuses it and `shot --gpu --cursor` works, measured: `x11grab` with and without the pointer differ on the new X screen. **All three found the dependency list incomplete**: the design writes the `Xwayland` cookie itself, with `xauth`, and `shot --gpu` never runs `wf-recorder`. The list now names both. One lane found the `--gpu` privacy figure was measured on the old recorder; it now says so and that it must be re-measured on `wf-recorder`. Orchestrator-found from open questions: a stale "A fix is planned." left by the arming edit, deleted; "no recorded frame showed" the pointer, when one sampled frame was checked, narrowed. One Q1 outside the change, filed as DEMO-0112 rather than fixed: the `vkcube` example of an app that is never found, false since DEMO-0043. Dismissed: the Status paragraph's `--gpu` verification predates the change, but nothing is built differently from it; it is refreshed when the build is verified. Resolved clean: `wf-recorder` 0.6 has no muxer-flag option (`-m` picks the muxer only); `cage` starts headless without `WLR_LIBINPUT_NO_DEVICES` (measured). **All three lanes disclosed they were not cold**: the harness injects main's pre-change `CLAUDE.md`, and the git snapshot named DEMO-0109 to DEMO-0111. |
| 7 | 2026-09-26 | adr (pinned) | 3 | 0 | 0 | 2 | 2 | 2 | Loop 2 of the DEMO-0111 run. Every lane held every question. Two lanes returned no findings. One lane found the privacy promise silent on the compositor's own socket, a second way to the `--gpu` picture that the X cookie does not cover. Measured: `cage` makes it in `/run/user/1000` (`drwx------`), so only the user's own programs reach it; the privacy bullet now says so. Orchestrator-found from two lanes' open questions: the blank check reads the X side while `record --gpu`'s video comes from the compositor, so a recorder writing black would pass. The design now also checks a frame of the finished video there. One lane re-found the `vkcube` example already filed as DEMO-0112; the brief failed to list it as already surfaced, so it is not counted. Resolved by measurement from an open question: without `WLR_LIBINPUT_NO_DEVICES`, `wf-recorder` showed no pointer in any of 39 frames while `x11grab` drew it where it was moved, and the parked pointer stayed parked on the new `Xwayland`. The pointer bullet now states that stronger measurement. Dismissed as building nothing different: the Status paragraph's `--gpu` checks predate the change (refreshed when the build is verified); "refreshed only a few times a second" is loose against 156 of 588. **All three lanes disclosed they were not cold**: main's pre-change `CLAUDE.md` injected, and the git snapshot named DEMO-0111 to DEMO-0113. |
| 8 | 2026-09-26 | adr (pinned) | 3 | 0 | 1 | 1 | 1 | 1 | Loop 3 of the DEMO-0111 run. **Cap reached (3 for an ADR); nothing verified is unfixed, so there is no tail.** Every lane held every question. **All three lanes found the one defect, and it was loop 2's own addition**: the check of the finished `record --gpu` video said neither which frame it samples nor whether step 6's exception applies, so one builder fails a run whose app closed itself and another passes it. Now it samples at the same moments as the two looks at the screen and is skipped in the same cases. Own-fix share of this loop: 1 of 1, on text loop 2 wrote. By the number that is the oscillating shape; on substance it is an unpropagated consequence (a new check that did not inherit the old checks' skips), which terminates once propagated. It ends this document's review either way: the build is the better next reviewer. Second share: 11 of the run's 12 verified findings (rows 6 to 8, the re-found `vkcube` excluded) fall inside the armed `--gpu` text; the one outside is filed as DEMO-0112. **All three lanes disclosed they were not cold**: main's pre-change `CLAUDE.md` injected, and the git snapshot named DEMO-0111 to DEMO-0113. |
